<!--
Say what a caller gets, not what the diff does. If it fixes a wrong reading,
the document that produced it is worth more than any description of the fix.
-->

## What changes, for whoever uses it

## Why

- [ ] `npm run gate` is green, coverage included
- [ ] A test fails without this change
- [ ] `CHANGELOG.md` says what changed, under `## [Unreleased]`
- [ ] No new runtime dependency
- [ ] Nothing here learns what a document _means_ — the caller still names the kind of a cell
